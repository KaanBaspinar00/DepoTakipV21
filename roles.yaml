roles:
  users:
    canan:
      password: "0"
      role: "admin"  # Changed from 'users' to 'role'
      name: "Canan"
    Mert:
      password: "0"
      role: "user"  # Changed from 'users' to 'role'
      name: "Mert"
